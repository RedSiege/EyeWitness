# Core dependencies for all platforms
netaddr>=0.10.0
psutil>=5.9.0

# This is what causes the error on the install script, you can't install externally managed modules like this, see changes in the setup.sh
# Lines 44,45 and 51, 52 did a hard apt get python-xyz works on a clean version of Kali/Debian and Ubuntu. 
#rapidfuzz>=3.0.0
#selenium>=4.29.0


# Platform-specific dependencies
# Virtual display support for Unix systems (Linux/macOS) only
pyvirtualdisplay>=3.0; sys_platform != "win32"
