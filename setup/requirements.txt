# EyeWitness Python Dependencies
# These packages are installed in a virtual environment to avoid PEP 668 issues

# Core networking and system utilities
netaddr>=0.10.0
psutil>=5.9.0

# Web automation and browser control  
selenium>=4.29.0
rapidfuzz>=3.0.0

# Virtual display support for Linux/macOS headless operation
pyvirtualdisplay>=3.0; sys_platform != "win32"

# Command line tab completion
argcomplete>=2.0.0

# Additional utilities for robust operation
requests>=2.28.0
urllib3>=1.26.0