# Core dependencies for all platforms
netaddr>=0.10.0
psutil>=5.9.0

# Critical dependencies - uncommented for Docker environment
# Native install uses apt packages to avoid PEP 668 issues
rapidfuzz>=3.0.0
selenium>=4.29.0

# Platform-specific dependencies
# Virtual display support for Unix systems (Linux/macOS) only
pyvirtualdisplay>=3.0; sys_platform != "win32"
